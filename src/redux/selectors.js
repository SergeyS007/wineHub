export const getTypeFilter = (state) => state.filters.type;

export const getPriceFilter = (state) => state.filters.price;

export const getCountryFilter = (state) => state.filters.country;

export const getRatingFilter = (state) => state.filters.rating;

export const getShowFilter = (state) => state.showFilters.show;

export const getShowModal = (state) => state.showModal.showModal;

export const getShowHero = (state) => state.showHero.showHero;

export const getShowRegister = (state) => state.showRegister.showRegister;

export const getShowSignIn = (state) => state.showRegister.showSignIn;
